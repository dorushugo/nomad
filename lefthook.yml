pre-commit:
  parallel: true
  commands:
    biome-format:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: npx @biomejs/biome format --no-errors-on-unmatched --write {staged_files}
      stage_fixed: true
